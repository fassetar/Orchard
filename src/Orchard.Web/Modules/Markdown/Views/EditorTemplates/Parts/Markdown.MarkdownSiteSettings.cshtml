@model Markdown.Models.MarkdownSiteSettingsPart

<fieldset>
    <legend>Markdown</legend>
    <div>
        @Html.EditorFor(m => m.UseMarkdownForBlogs) 
        <label class="forcheckbox" for="@Html.FieldIdFor(m => m.UseMarkdownForBlogs)">Use Markdown to write Blog Post</label>
        <span class="help-block">Enable to change the default editor for all blog posts to Markdown.</span>
    </div>
</fieldset>