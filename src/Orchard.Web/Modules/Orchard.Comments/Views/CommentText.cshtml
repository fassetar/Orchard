@using Orchard.Comments.Models
@{
    CommentPart comment = Model.ContentPart;
}

<p class="form-control">@(new MvcHtmlString(Html.Encode(comment.Record.CommentText).Replace("\r\n", "<br />\r\n")))</p>
